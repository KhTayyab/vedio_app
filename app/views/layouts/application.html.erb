<!DOCTYPE html>
<html>
  <head>
    <title>Sample Video</title>
    <link rel="icon" type="image/ico" href="simply_video.png" />
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  	<nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <a class="navbar-brand" href="#">Sample Video</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
          	<li class="video_configrations active"><%= link_to  "Video", video_configrations_path(:shop => params[:shop]) %></li>
            <li class="app_configrations"><%= link_to  "Configrations", app_configrations_path(:shop => params[:shop]) %></li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>
    <div style="margin-top: 6%;">
      <%= yield %>
    </div>
  </body>
</html>

<script type="text/javascript">
	$(function()
	{
		var path = window.location.pathname
		if (path == "/video_configrations") {
			$('.video_configrations').addClass('active')
		}
	});
</script>

<script type="text/javascript">
  $(function()
  {
    var path = window.location.pathname
    if (path == "/video_configrations") {
      $('.video_configrations').addClass('active')
      $('.app_configrations').removeClass('active')
      $('.sms_buckets').removeClass('active')
    }
    else if (path == "/app_configrations") {
      $('.video_configrations').removeClass('active')
      $('.app_configrations').addClass('active')
    }
    else
    {
      $('.video_configrations').addClass('active')
      $('.app_configrations').removeClass('active')
    }
  });
</script>

